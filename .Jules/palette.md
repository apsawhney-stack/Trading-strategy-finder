## 2026-01-04 - Discovery Checkbox Accessibility\n**Learning:** Interactive list items with checkboxes often lack labels for the checkbox itself when the entire row is clickable. Adding `aria-label` to the checkbox that references the item title bridges this gap for screen reader users without visual clutter.\n**Action:** Always check inner interactive elements (like checkboxes) in clickable rows for accessible names.
