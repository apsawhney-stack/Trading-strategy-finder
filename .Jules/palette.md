## 2024-05-22 - [Accessible URL Input Pattern]
**Learning:** Adding hidden labels and correct input types (e.g., `type="url"`) significantly improves accessibility and mobile UX without altering the visual design.
**Action:** Always check for missing labels in form inputs and use `.sr-only` labels combined with specific input types for better semantic HTML.
